[package]
name = "Tagger"
version = "0.1.0"
edition = "2024"
authors = ["Madelyn Belmen"]
description = "A CLI file-tagging system"

[dependencies]
clap = { version = "4.6.1", features = ["cargo"] }
directories = "6.0.0"
